<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie search</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@700&family=Montserrat:wght@100;200;300;400;500;600;700;800;900&family=Noto+Serif&family=Ubuntu:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <h1><span id="M">M</span>OVIE BUFF</h1>
    <div class="container">
        <div class="card" style="width: 18rem;">
            <% if(locals.movie){ %>
            <img class="card-img-top" src=<%= movie.image.url %> alt="Card image cap">
            <div class="card-body">
              <h5 class="card-title"><span class="lab">Title :</span><%= movie.title %></h5>
              <h6 class="card-title"><span class="lab">Year :</span><%= movie.year %></h6>
              <h6 class="card-title"><span class="lab">Rating :</span><%= rating %></h6>
              <% } else if(locals.error) {%>
                <h2>
                    <%= error %>
                    try again!!
                </h2>
              <% } else { %>
                <form action="/" method="post">
                    <label for="title" id="label">Enter Title</label>
                    <input type="text" name="title" id="input">
                    <button class="btn btn-primary btn-lg" type="submit">SEARCH</button>
                </form>
              <% } %>
            </div>
          </div>
    </div>

</body>
</html>